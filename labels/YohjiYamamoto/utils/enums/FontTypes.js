/**
 * Initialization of enum FontTypes.
 *
 * Initialization as enum of font types.
 *
 * @author Etienne Bolduc
 */

/**
 * Enum for font types.
 * @readonly
 * @enum {string}
 */
const FontTypes = Object.freeze({
    SERIF: "SERIF",
    SANS_SERIF: "SANS_SERIF"
});

export default FontTypes;

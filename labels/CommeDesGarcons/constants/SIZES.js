/**
 * Initialization of enum Sizes.
 *
 * Initialization as enum of size notation found in monthly product code patterns
 * and their corresponding garment size.
 *
 * @author Etienne Bolduc
 */

/**
 * Enum for size notation in the monthly framework.
 * @readonly
 * @enum    {string}
 */
const SIZES = Object.freeze({
    0: null,
    S: "S",
    M: "M",
    L: "L",
    X: null,
});

export default SIZES;

/**
 * Initialization of enum Sizes.
 *
 * Initialization as enum of the COMME des GARÇONS size notations found in
 * monthly product code pattern and their corresponding garment size.
 *
 * @author Etienne Bolduc
 */

/**
 * Enum for COMME des GARÇONS size notations in the monthly product code pattern.
 * @readonly
 * @enum    {string}
 */
const SIZES = Object.freeze({
    0: null,
    S: "S",
    M: "M",
    L: "L",
    X: null,
});

export default SIZES;

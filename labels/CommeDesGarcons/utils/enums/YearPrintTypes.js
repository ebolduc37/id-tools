/**
 * Initialization of enum YearPrintTypes.
 *
 * Initialization as enum of year print types.
 *
 * @author Etienne Bolduc
 */

/**
 * Enum for year print types.
 * @readonly
 * @enum    {string}
 */
const YearPrintTypes = Object.freeze({
    NUMERIC: "NUMERIC",
    BLANK: "BLANK",
    ERASED: "ERASED",
    WRONG: "WRONG",
});

export default YearPrintTypes;

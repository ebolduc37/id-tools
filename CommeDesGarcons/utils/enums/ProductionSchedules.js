/**
 * Initialization of enum ProductionSchedules.
 *
 * Initialization as enum of the production schedules.
 *
 * @author Etienne Bolduc
 */

/**
 * Enum for production schedule types.
 * @readonly
 * @enum    {string}
 */
const ProductionSchedules = Object.freeze({
    EARLY: "EARLY",
    LATE: "LATE",
})

export default ProductionSchedules;
